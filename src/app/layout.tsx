import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'TrueCheck - Verification & Validation System',
  description: 'A modern verification and validation system for ensuring data integrity and accuracy.',
  keywords: ['verification', 'validation', 'checking', 'data integrity'],
  authors: [{ name: 'TrueCheck Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  )
} 